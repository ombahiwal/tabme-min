# MongoDB connection string (use MongoDB Atlas for production)
MONGODB_URI=mongodb+srv://username:password@cluster.mongodb.net/tabme?retryWrites=true&w=majority

# JWT Secret for authentication (use a strong random string in production)
JWT_SECRET=your-super-secret-jwt-key-change-in-production

# Base URL for the application (update to your Vercel domain in production)
NEXT_PUBLIC_BASE_URL=http://localhost:3000

# Node environment
NODE_ENV=development
